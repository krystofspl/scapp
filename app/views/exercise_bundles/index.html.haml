- content_for :heading do
  = "#{t('exercise_bundle.index.exercise_bundles_list')}"

- breadcrumb :exercise_bundles

#action-box
  = link_to t('exercise_bundle.show.view_my_bundles'), user_exercise_bundles_path(current_user), class: 'btn btn-info'
  = link_to new_exercise_bundle_path, class: 'btn btn-info' do
    %i.fa.fa-plus
    = t('exercise_bundle.new.new_bundle')

= render :template => 'exercise_bundles/_list_table.html.haml', :locale => @locale
