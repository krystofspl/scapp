$(function() {
    $("#exercise-set-setups-used-wrapper")
            .html("<%= escape_javascript(render partial: 'exercise_set_realization_setups/list_small', locals: {:exercise_set_realization_setups => @exercise_set_realization_setups}) %>")
            .find('li').disableSelection();
    $("#exercise-set-realization-setups-accordion").accordion({
        collapsible: true,
        autoHeight: false,
        header: '.accordion-header-wrapper'
    });
});